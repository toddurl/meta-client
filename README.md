# meta-client
A reference implementation of the Meta‑Client Pattern, treating Azure as a Meta‑Computer and the browser as the orchestration layer. Uses Blazor + Microsoft Graph to show how client‑side APIs replace traditional backends with scalable, secure, AI‑native cloud primitives.
